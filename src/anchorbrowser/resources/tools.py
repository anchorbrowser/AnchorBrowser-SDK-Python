# Generated by scripts/generate from spec/openapi.yaml + spec/sdk-manifest.yaml — do not edit.

from __future__ import annotations

from typing import Dict, Literal

import httpx

from ..types import models
from .._types import Body, Query, Headers, NotGiven, not_given
from .._utils import path_template, strip_not_given
from .._resource import SyncAPIResource, AsyncAPIResource
from .._response import BinaryAPIResponse, AsyncBinaryAPIResponse
from .._base_client import make_request_options

__all__ = ["ToolsResource", "AsyncToolsResource"]


class ToolsResource(SyncAPIResource):
    def perform_web_task(
        self,
        *,
        prompt: str,
        url: str | NotGiven = not_given,
        agent: Literal["browser-use", "openai-cua", "gemini-computer-use", "anthropic-cua", "yutori"]
        | NotGiven = not_given,
        provider: Literal["openai", "gemini", "groq", "azure", "xai"] | NotGiven = not_given,
        model: str | NotGiven = not_given,
        detect_elements: bool | NotGiven = not_given,
        human_intervention: bool | NotGiven = not_given,
        max_steps: int | NotGiven = not_given,
        secret_values: Dict[str, object] | NotGiven = not_given,
        highlight_elements: bool | NotGiven = not_given,
        output_schema: Dict[str, object] | NotGiven = not_given,
        async_: bool | NotGiven = not_given,
        session_id: str | NotGiven = not_given,
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> models.PerformWebTaskResponseSchema:
        """
        Perform Web Task

        Start from a URL and perform the given task.

        Args:
          url: The URL of the webpage. If not provided, the tool will use the current page in the
          session.
          prompt: The task to be autonomously completed.
          agent: The AI agent to use for task completion. Defaults to browser-use.
          provider: The AI provider to use for task completion.
          model: The specific model to use for task completion. see our
          [models](/agentic-browser-control/ai-task-completion#available-models) page for more
          information.
          detect_elements: Enable element detection for better interaction accuracy. Improves the
          agent's ability to identify and interact with UI elements.
          human_intervention: Allow human intervention during task execution. When enabled, the
          agent can request human input for ambiguous situations.
          max_steps: Maximum number of steps the agent can take to complete the task. Defaults to
          200.
          secret_values: Secret values to pass to the agent for secure credential handling. Keys
          and values are passed as environment variables to the agent.
          highlight_elements: Whether to highlight elements during task execution for better
          visibility.
          output_schema: JSON Schema defining the expected structure of the output data.
          async_: Whether to run the task asynchronously. If true, the task will be run
          asynchronously and the response will include a workflow ID.
          session_id: An optional browser session identifier to reference an existing running
          browser sessions. When passed, the tool will be executed on the provided browser session.
        """
        return self._post(
            "/v1/tools/perform-web-task",
            body=strip_not_given(
                {
                    "url": url,
                    "prompt": prompt,
                    "agent": agent,
                    "provider": provider,
                    "model": model,
                    "detect_elements": detect_elements,
                    "human_intervention": human_intervention,
                    "max_steps": max_steps,
                    "secret_values": secret_values,
                    "highlight_elements": highlight_elements,
                    "output_schema": output_schema,
                    "async": async_,
                }
            ),
            options=make_request_options(
                extra_headers=extra_headers,
                extra_query=extra_query,
                extra_body=extra_body,
                timeout=timeout,
                query=strip_not_given({"sessionId": session_id}),
            ),
            cast_to=models.PerformWebTaskResponseSchema,
        )

    def get_perform_web_task_status(
        self,
        workflow_id: str,
        *,
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> models.PerformWebTaskStatusResponseSchema:
        """
        Get Perform Web Task Status

        Get the status of an asynchronous perform-web-task execution by workflow ID.

        Args:
          workflow_id: The workflow ID returned when starting an asynchronous perform-web-task
          execution.
        """
        if not workflow_id:
            raise ValueError(f"Expected a non-empty value for `workflow_id` but received {workflow_id!r}")
        return self._get(
            path_template("/v1/tools/perform-web-task/{workflowId}/status", workflowId=workflow_id),
            options=make_request_options(
                extra_headers=extra_headers, extra_query=extra_query, extra_body=extra_body, timeout=timeout
            ),
            cast_to=models.PerformWebTaskStatusResponseSchema,
        )

    def execute_code(
        self,
        *,
        code: str,
        session_id: str,
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> models.ExecuteCodeToolResponseSchema:
        """
        Execute Code Snippet

        Run a TypeScript snippet in the sandbox against the browser session (query `sessionId`
        required).

        Args:
          code: TypeScript snippet (async function body) with injected page, browser, context,
          sessionId, anchorClient.
          session_id: Browser session to run the snippet against.
        """
        return self._post(
            "/v1/tools/execute-code",
            body=strip_not_given({"code": code}),
            options=make_request_options(
                extra_headers=extra_headers,
                extra_query=extra_query,
                extra_body=extra_body,
                timeout=timeout,
                query=strip_not_given({"sessionId": session_id}),
            ),
            cast_to=models.ExecuteCodeToolResponseSchema,
        )

    def fetch_webpage(
        self,
        *,
        url: str,
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> str:
        """
        Web Unlocker

        Fetch fully-rendered page content from any URL — including bot-protected sites — without
        managing a browser session.
        Requests are routed through stealth browsers with residential proxies, captcha solving, and
        fingerprint randomization.
        No session is required. See the [Web Unlocker guide](/advanced/web-unlocker) for more
        details.

        Args:
          url: The fully-qualified URL to fetch (e.g. `https://www.linkedin.com/company/openai`).
        """
        extra_headers = {"Accept": "text/html", **(extra_headers or {})}
        return self._post(
            "/v1/tools/fetch/webpage",
            body=strip_not_given({"url": url}),
            options=make_request_options(
                extra_headers=extra_headers, extra_query=extra_query, extra_body=extra_body, timeout=timeout
            ),
            cast_to=str,
        )

    def screenshot_webpage(
        self,
        *,
        url: str | NotGiven = not_given,
        width: int | NotGiven = not_given,
        height: int | NotGiven = not_given,
        image_quality: int | NotGiven = not_given,
        wait: int | NotGiven = not_given,
        scroll_all_content: bool | NotGiven = not_given,
        capture_full_height: bool | NotGiven = not_given,
        s3_target_address: str | NotGiven = not_given,
        session_id: str | NotGiven = not_given,
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> BinaryAPIResponse:
        """
        Screenshot Webpage

        This endpoint captures a screenshot of the specified webpage using Chromium. Users can
        customize the viewport dimensions and capture options.

        Args:
          url: The URL of the webpage to capture.
          width: The width of the browser viewport in pixels.
          height: The height of the browser viewport in pixels.
          image_quality: Quality of the output image, on the range 1-100. 100 will not perform any
          compression.
          wait: Duration in milliseconds to wait after page has loaded, mainly used for sites with
          JS animations.
          scroll_all_content: If true, scrolls the page and captures all visible content.
          capture_full_height: If true, captures the entire height of the page, ignoring the
          viewport height.
          s3_target_address: Presigned S3 url target to upload the image to.
          session_id: An optional browser session identifier to reference an existing running
          browser sessions. When passed, the tool will be executed on the provided browser session.
        """
        extra_headers = {"Accept": "image/png", **(extra_headers or {})}
        return self._post(
            "/v1/tools/screenshot",
            body=strip_not_given(
                {
                    "url": url,
                    "width": width,
                    "height": height,
                    "image_quality": image_quality,
                    "wait": wait,
                    "scroll_all_content": scroll_all_content,
                    "capture_full_height": capture_full_height,
                    "s3_target_address": s3_target_address,
                }
            ),
            options=make_request_options(
                extra_headers=extra_headers,
                extra_query=extra_query,
                extra_body=extra_body,
                timeout=timeout,
                query=strip_not_given({"sessionId": session_id}),
            ),
            cast_to=BinaryAPIResponse,
        )

    def create_page_pdf(
        self,
        *,
        url: str,
        landscape: bool | NotGiven = not_given,
        display_header_footer: bool | NotGiven = not_given,
        print_background: bool | NotGiven = not_given,
        scale: float | NotGiven = not_given,
        paper_width: float | NotGiven = not_given,
        paper_height: float | NotGiven = not_given,
        margin_top: float | NotGiven = not_given,
        margin_bottom: float | NotGiven = not_given,
        margin_left: float | NotGiven = not_given,
        margin_right: float | NotGiven = not_given,
        page_ranges: str | NotGiven = not_given,
        header_template: str | NotGiven = not_given,
        footer_template: str | NotGiven = not_given,
        prefer_css_page_size: bool | NotGiven = not_given,
        session_id: str | NotGiven = not_given,
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> BinaryAPIResponse:
        """
        Get Page PDF

        Generates a PDF of the current page for a browser session and returns it as a binary PDF
        file.

        Args:
          url: The URL of the page to print to PDF.
          landscape: Paper orientation. Defaults to false.
          display_header_footer: Display header and footer. Defaults to false.
          print_background: Print background graphics. Defaults to false.
          scale: Scale of the webpage rendering. Defaults to 1.
          paper_width: Paper width in inches. Defaults to 8.5.
          paper_height: Paper height in inches. Defaults to 11.
          margin_top: Top margin in inches. Defaults to ~0.4.
          margin_bottom: Bottom margin in inches. Defaults to ~0.4.
          margin_left: Left margin in inches. Defaults to ~0.4.
          margin_right: Right margin in inches. Defaults to ~0.4.
          page_ranges: Page ranges to print, e.g. '1-5, 8, 11-13'. Defaults to all pages.
          header_template: HTML template for the print header.
          footer_template: HTML template for the print footer.
          prefer_css_page_size: Prefer page size as defined by CSS. Defaults to false.
          session_id: Optional browser session ID. If omitted, `url` in the request body is
          required.
        """
        extra_headers = {"Accept": "application/pdf", **(extra_headers or {})}
        return self._post(
            "/v1/tools/page-pdf",
            body=strip_not_given(
                {
                    "url": url,
                    "landscape": landscape,
                    "displayHeaderFooter": display_header_footer,
                    "printBackground": print_background,
                    "scale": scale,
                    "paperWidth": paper_width,
                    "paperHeight": paper_height,
                    "marginTop": margin_top,
                    "marginBottom": margin_bottom,
                    "marginLeft": margin_left,
                    "marginRight": margin_right,
                    "pageRanges": page_ranges,
                    "headerTemplate": header_template,
                    "footerTemplate": footer_template,
                    "preferCSSPageSize": prefer_css_page_size,
                }
            ),
            options=make_request_options(
                extra_headers=extra_headers,
                extra_query=extra_query,
                extra_body=extra_body,
                timeout=timeout,
                query=strip_not_given({"sessionId": session_id}),
            ),
            cast_to=BinaryAPIResponse,
        )


class AsyncToolsResource(AsyncAPIResource):
    async def perform_web_task(
        self,
        *,
        prompt: str,
        url: str | NotGiven = not_given,
        agent: Literal["browser-use", "openai-cua", "gemini-computer-use", "anthropic-cua", "yutori"]
        | NotGiven = not_given,
        provider: Literal["openai", "gemini", "groq", "azure", "xai"] | NotGiven = not_given,
        model: str | NotGiven = not_given,
        detect_elements: bool | NotGiven = not_given,
        human_intervention: bool | NotGiven = not_given,
        max_steps: int | NotGiven = not_given,
        secret_values: Dict[str, object] | NotGiven = not_given,
        highlight_elements: bool | NotGiven = not_given,
        output_schema: Dict[str, object] | NotGiven = not_given,
        async_: bool | NotGiven = not_given,
        session_id: str | NotGiven = not_given,
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> models.PerformWebTaskResponseSchema:
        """
        Perform Web Task

        Start from a URL and perform the given task.

        Args:
          url: The URL of the webpage. If not provided, the tool will use the current page in the
          session.
          prompt: The task to be autonomously completed.
          agent: The AI agent to use for task completion. Defaults to browser-use.
          provider: The AI provider to use for task completion.
          model: The specific model to use for task completion. see our
          [models](/agentic-browser-control/ai-task-completion#available-models) page for more
          information.
          detect_elements: Enable element detection for better interaction accuracy. Improves the
          agent's ability to identify and interact with UI elements.
          human_intervention: Allow human intervention during task execution. When enabled, the
          agent can request human input for ambiguous situations.
          max_steps: Maximum number of steps the agent can take to complete the task. Defaults to
          200.
          secret_values: Secret values to pass to the agent for secure credential handling. Keys
          and values are passed as environment variables to the agent.
          highlight_elements: Whether to highlight elements during task execution for better
          visibility.
          output_schema: JSON Schema defining the expected structure of the output data.
          async_: Whether to run the task asynchronously. If true, the task will be run
          asynchronously and the response will include a workflow ID.
          session_id: An optional browser session identifier to reference an existing running
          browser sessions. When passed, the tool will be executed on the provided browser session.
        """
        return await self._post(
            "/v1/tools/perform-web-task",
            body=strip_not_given(
                {
                    "url": url,
                    "prompt": prompt,
                    "agent": agent,
                    "provider": provider,
                    "model": model,
                    "detect_elements": detect_elements,
                    "human_intervention": human_intervention,
                    "max_steps": max_steps,
                    "secret_values": secret_values,
                    "highlight_elements": highlight_elements,
                    "output_schema": output_schema,
                    "async": async_,
                }
            ),
            options=make_request_options(
                extra_headers=extra_headers,
                extra_query=extra_query,
                extra_body=extra_body,
                timeout=timeout,
                query=strip_not_given({"sessionId": session_id}),
            ),
            cast_to=models.PerformWebTaskResponseSchema,
        )

    async def get_perform_web_task_status(
        self,
        workflow_id: str,
        *,
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> models.PerformWebTaskStatusResponseSchema:
        """
        Get Perform Web Task Status

        Get the status of an asynchronous perform-web-task execution by workflow ID.

        Args:
          workflow_id: The workflow ID returned when starting an asynchronous perform-web-task
          execution.
        """
        if not workflow_id:
            raise ValueError(f"Expected a non-empty value for `workflow_id` but received {workflow_id!r}")
        return await self._get(
            path_template("/v1/tools/perform-web-task/{workflowId}/status", workflowId=workflow_id),
            options=make_request_options(
                extra_headers=extra_headers, extra_query=extra_query, extra_body=extra_body, timeout=timeout
            ),
            cast_to=models.PerformWebTaskStatusResponseSchema,
        )

    async def execute_code(
        self,
        *,
        code: str,
        session_id: str,
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> models.ExecuteCodeToolResponseSchema:
        """
        Execute Code Snippet

        Run a TypeScript snippet in the sandbox against the browser session (query `sessionId`
        required).

        Args:
          code: TypeScript snippet (async function body) with injected page, browser, context,
          sessionId, anchorClient.
          session_id: Browser session to run the snippet against.
        """
        return await self._post(
            "/v1/tools/execute-code",
            body=strip_not_given({"code": code}),
            options=make_request_options(
                extra_headers=extra_headers,
                extra_query=extra_query,
                extra_body=extra_body,
                timeout=timeout,
                query=strip_not_given({"sessionId": session_id}),
            ),
            cast_to=models.ExecuteCodeToolResponseSchema,
        )

    async def fetch_webpage(
        self,
        *,
        url: str,
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> str:
        """
        Web Unlocker

        Fetch fully-rendered page content from any URL — including bot-protected sites — without
        managing a browser session.
        Requests are routed through stealth browsers with residential proxies, captcha solving, and
        fingerprint randomization.
        No session is required. See the [Web Unlocker guide](/advanced/web-unlocker) for more
        details.

        Args:
          url: The fully-qualified URL to fetch (e.g. `https://www.linkedin.com/company/openai`).
        """
        extra_headers = {"Accept": "text/html", **(extra_headers or {})}
        return await self._post(
            "/v1/tools/fetch/webpage",
            body=strip_not_given({"url": url}),
            options=make_request_options(
                extra_headers=extra_headers, extra_query=extra_query, extra_body=extra_body, timeout=timeout
            ),
            cast_to=str,
        )

    async def screenshot_webpage(
        self,
        *,
        url: str | NotGiven = not_given,
        width: int | NotGiven = not_given,
        height: int | NotGiven = not_given,
        image_quality: int | NotGiven = not_given,
        wait: int | NotGiven = not_given,
        scroll_all_content: bool | NotGiven = not_given,
        capture_full_height: bool | NotGiven = not_given,
        s3_target_address: str | NotGiven = not_given,
        session_id: str | NotGiven = not_given,
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> AsyncBinaryAPIResponse:
        """
        Screenshot Webpage

        This endpoint captures a screenshot of the specified webpage using Chromium. Users can
        customize the viewport dimensions and capture options.

        Args:
          url: The URL of the webpage to capture.
          width: The width of the browser viewport in pixels.
          height: The height of the browser viewport in pixels.
          image_quality: Quality of the output image, on the range 1-100. 100 will not perform any
          compression.
          wait: Duration in milliseconds to wait after page has loaded, mainly used for sites with
          JS animations.
          scroll_all_content: If true, scrolls the page and captures all visible content.
          capture_full_height: If true, captures the entire height of the page, ignoring the
          viewport height.
          s3_target_address: Presigned S3 url target to upload the image to.
          session_id: An optional browser session identifier to reference an existing running
          browser sessions. When passed, the tool will be executed on the provided browser session.
        """
        extra_headers = {"Accept": "image/png", **(extra_headers or {})}
        return await self._post(
            "/v1/tools/screenshot",
            body=strip_not_given(
                {
                    "url": url,
                    "width": width,
                    "height": height,
                    "image_quality": image_quality,
                    "wait": wait,
                    "scroll_all_content": scroll_all_content,
                    "capture_full_height": capture_full_height,
                    "s3_target_address": s3_target_address,
                }
            ),
            options=make_request_options(
                extra_headers=extra_headers,
                extra_query=extra_query,
                extra_body=extra_body,
                timeout=timeout,
                query=strip_not_given({"sessionId": session_id}),
            ),
            cast_to=AsyncBinaryAPIResponse,
        )

    async def create_page_pdf(
        self,
        *,
        url: str,
        landscape: bool | NotGiven = not_given,
        display_header_footer: bool | NotGiven = not_given,
        print_background: bool | NotGiven = not_given,
        scale: float | NotGiven = not_given,
        paper_width: float | NotGiven = not_given,
        paper_height: float | NotGiven = not_given,
        margin_top: float | NotGiven = not_given,
        margin_bottom: float | NotGiven = not_given,
        margin_left: float | NotGiven = not_given,
        margin_right: float | NotGiven = not_given,
        page_ranges: str | NotGiven = not_given,
        header_template: str | NotGiven = not_given,
        footer_template: str | NotGiven = not_given,
        prefer_css_page_size: bool | NotGiven = not_given,
        session_id: str | NotGiven = not_given,
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> AsyncBinaryAPIResponse:
        """
        Get Page PDF

        Generates a PDF of the current page for a browser session and returns it as a binary PDF
        file.

        Args:
          url: The URL of the page to print to PDF.
          landscape: Paper orientation. Defaults to false.
          display_header_footer: Display header and footer. Defaults to false.
          print_background: Print background graphics. Defaults to false.
          scale: Scale of the webpage rendering. Defaults to 1.
          paper_width: Paper width in inches. Defaults to 8.5.
          paper_height: Paper height in inches. Defaults to 11.
          margin_top: Top margin in inches. Defaults to ~0.4.
          margin_bottom: Bottom margin in inches. Defaults to ~0.4.
          margin_left: Left margin in inches. Defaults to ~0.4.
          margin_right: Right margin in inches. Defaults to ~0.4.
          page_ranges: Page ranges to print, e.g. '1-5, 8, 11-13'. Defaults to all pages.
          header_template: HTML template for the print header.
          footer_template: HTML template for the print footer.
          prefer_css_page_size: Prefer page size as defined by CSS. Defaults to false.
          session_id: Optional browser session ID. If omitted, `url` in the request body is
          required.
        """
        extra_headers = {"Accept": "application/pdf", **(extra_headers or {})}
        return await self._post(
            "/v1/tools/page-pdf",
            body=strip_not_given(
                {
                    "url": url,
                    "landscape": landscape,
                    "displayHeaderFooter": display_header_footer,
                    "printBackground": print_background,
                    "scale": scale,
                    "paperWidth": paper_width,
                    "paperHeight": paper_height,
                    "marginTop": margin_top,
                    "marginBottom": margin_bottom,
                    "marginLeft": margin_left,
                    "marginRight": margin_right,
                    "pageRanges": page_ranges,
                    "headerTemplate": header_template,
                    "footerTemplate": footer_template,
                    "preferCSSPageSize": prefer_css_page_size,
                }
            ),
            options=make_request_options(
                extra_headers=extra_headers,
                extra_query=extra_query,
                extra_body=extra_body,
                timeout=timeout,
                query=strip_not_given({"sessionId": session_id}),
            ),
            cast_to=AsyncBinaryAPIResponse,
        )
