# Generated by scripts/generate from spec/openapi.yaml + spec/sdk-manifest.yaml — do not edit.

from __future__ import annotations

from typing import Literal, Iterable

import httpx

from ..types import models
from .._types import Body, Query, Headers, NotGiven, not_given
from .._utils import path_template, strip_not_given
from .._resource import SyncAPIResource, AsyncAPIResource
from .._base_client import make_request_options

__all__ = ["WebhooksResource", "AsyncWebhooksResource"]


class WebhooksResource(SyncAPIResource):
    def create_webhook(
        self,
        *,
        url: str,
        subscribed_events: Iterable[
            Literal[
                "task.completed",
                "task.failed",
                "task.cancelled",
                "task.healed",
                "session.completed",
                "session.failed",
                "session.recording.ready",
                "batch.completed",
                "batch.failed",
                "intervention.requested",
                "intervention.resolved",
                "identity.authenticated",
                "identity.authentication_failed",
            ]
        ],
        description: str | NotGiven = not_given,
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> models.WebhookWithSecret:
        """
        Create Webhook

        Register a new HTTPS endpoint to receive events. The response includes
        a one-time `secret` (HMAC-SHA256 signing key) — store it; Anchor never
        returns it again. Up to 5 webhooks per project.

        Args:
          url: HTTPS endpoint Anchor will POST events to. Rejected (400) if it points at
          loopback / private / link-local IPs, cloud metadata services, internal-only
          TLDs, AWS STS / IAM / EKS endpoints, or contains basic-auth credentials.
          description: Optional human-readable description.
          subscribed_events: Events this webhook should receive. At least one is required.
        """
        return self._post(
            "/v1/webhooks",
            body=strip_not_given({"url": url, "description": description, "subscribed_events": subscribed_events}),
            options=make_request_options(
                extra_headers=extra_headers, extra_query=extra_query, extra_body=extra_body, timeout=timeout
            ),
            cast_to=models.WebhookWithSecret,
        )

    def list_webhooks(
        self,
        *,
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> models.WebhookListResponse:
        """
        List Webhooks

        List every active webhook for the authenticated project. The signing secret is never
        included.
        """
        return self._get(
            "/v1/webhooks",
            options=make_request_options(
                extra_headers=extra_headers, extra_query=extra_query, extra_body=extra_body, timeout=timeout
            ),
            cast_to=models.WebhookListResponse,
        )

    def get_webhook(
        self,
        id: str,
        *,
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> models.WebhookPublic:
        """
        Get Webhook

        Fetch a single webhook config. The signing secret is never returned.

        Args:
          id: Webhook id.
        """
        if not id:
            raise ValueError(f"Expected a non-empty value for `id` but received {id!r}")
        return self._get(
            path_template("/v1/webhooks/{id}", id=id),
            options=make_request_options(
                extra_headers=extra_headers, extra_query=extra_query, extra_body=extra_body, timeout=timeout
            ),
            cast_to=models.WebhookPublic,
        )

    def update_webhook(
        self,
        id: str,
        *,
        url: str | NotGiven = not_given,
        description: str | NotGiven = not_given,
        subscribed_events: Iterable[
            Literal[
                "task.completed",
                "task.failed",
                "task.cancelled",
                "task.healed",
                "session.completed",
                "session.failed",
                "session.recording.ready",
                "batch.completed",
                "batch.failed",
                "intervention.requested",
                "intervention.resolved",
                "identity.authenticated",
                "identity.authentication_failed",
            ]
        ]
        | NotGiven = not_given,
        enabled: bool | NotGiven = not_given,
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> models.WebhookPublic:
        """
        Update Webhook

        Partial update — pass only the fields you want to change. Setting `enabled: false`
        immediately stops events from fanning out to this webhook (existing in-flight
        deliveries finish their attempts).

        Args:
          id: Webhook id.
        """
        if not id:
            raise ValueError(f"Expected a non-empty value for `id` but received {id!r}")
        return self._patch(
            path_template("/v1/webhooks/{id}", id=id),
            body=strip_not_given(
                {"url": url, "description": description, "subscribed_events": subscribed_events, "enabled": enabled}
            ),
            options=make_request_options(
                extra_headers=extra_headers, extra_query=extra_query, extra_body=extra_body, timeout=timeout
            ),
            cast_to=models.WebhookPublic,
        )

    def delete_webhook(
        self,
        id: str,
        *,
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> models.WebhookDeleteResponse:
        """
        Delete Webhook

        Soft-delete a webhook. New events stop fanning out immediately. The events
        history (`/v1/webhooks/{id}/events`) is still queryable for the retention
        window even after delete.

        Args:
          id: Webhook id.
        """
        if not id:
            raise ValueError(f"Expected a non-empty value for `id` but received {id!r}")
        return self._delete(
            path_template("/v1/webhooks/{id}", id=id),
            options=make_request_options(
                extra_headers=extra_headers, extra_query=extra_query, extra_body=extra_body, timeout=timeout
            ),
            cast_to=models.WebhookDeleteResponse,
        )

    def rotate_webhook_secret(
        self,
        id: str,
        *,
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> models.WebhookWithSecret:
        """
        Rotate Webhook Signing Secret

        Mint a new HMAC-SHA256 signing secret. The previous secret remains valid
        for 24 hours so you can update your verification code at your own pace,
        then drop the old secret.

        The new secret is returned exactly once, in the `secret` field.

        Args:
          id: Webhook id.
        """
        if not id:
            raise ValueError(f"Expected a non-empty value for `id` but received {id!r}")
        return self._post(
            path_template("/v1/webhooks/{id}/rotate-secret", id=id),
            options=make_request_options(
                extra_headers=extra_headers, extra_query=extra_query, extra_body=extra_body, timeout=timeout
            ),
            cast_to=models.WebhookWithSecret,
        )

    def send_webhook_test_event(
        self,
        id: str,
        *,
        event_type: Literal[
            "task.completed",
            "task.failed",
            "task.cancelled",
            "task.healed",
            "session.completed",
            "session.failed",
            "session.recording.ready",
            "batch.completed",
            "batch.failed",
            "intervention.requested",
            "intervention.resolved",
            "identity.authenticated",
            "identity.authentication_failed",
        ],
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> models.WebhookTestResponse:
        """
        Send Test Event

        Fire a synthetic event of the given type through the real signing + retry
        path. Useful during development for confirming your endpoint can verify
        the signature and is reachable from Anchor's egress IPs.

        The synthetic delivery counts toward your delivery log so you can see it
        in `GET /v1/webhooks/{id}/events`.

        Args:
          id: Webhook id.
          event_type: Catalog of webhook event types Anchor can deliver. Adding a new event
          type is a coordinated change between session-manager (publisher),
          webhook-dispatcher (consumer), and the dashboard.
        """
        if not id:
            raise ValueError(f"Expected a non-empty value for `id` but received {id!r}")
        return self._post(
            path_template("/v1/webhooks/{id}/test", id=id),
            body=strip_not_given({"event_type": event_type}),
            options=make_request_options(
                extra_headers=extra_headers, extra_query=extra_query, extra_body=extra_body, timeout=timeout
            ),
            cast_to=models.WebhookTestResponse,
        )

    def list_webhook_events(
        self,
        id: str,
        *,
        limit: int | NotGiven = not_given,
        offset: int | NotGiven = not_given,
        status: Literal["pending", "in_flight", "succeeded", "failed", "dead"] | NotGiven = not_given,
        event_type: Literal[
            "task.completed",
            "task.failed",
            "task.cancelled",
            "task.healed",
            "session.completed",
            "session.failed",
            "session.recording.ready",
            "batch.completed",
            "batch.failed",
            "intervention.requested",
            "intervention.resolved",
            "identity.authenticated",
            "identity.authentication_failed",
        ]
        | NotGiven = not_given,
        since: str | NotGiven = not_given,
        until: str | NotGiven = not_given,
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> models.WebhookEventsListResponse:
        """
        List Webhook Deliveries

        Paginated history of every delivery attempt for this webhook (succeeded,
        failed, or dead-lettered). Filter by status, event type, or time range.
        Retention: 7 days.

        Args:
          id: Webhook id.
          limit: Page size, 1–100. Defaults to 25.
          offset: Number of rows to skip from the beginning of the result set.
          status: Filter by delivery status.
          event_type: Filter by event type.
          since: Only include deliveries scheduled at or after this ISO 8601 timestamp.
          until: Only include deliveries scheduled at or before this ISO 8601 timestamp.
        """
        if not id:
            raise ValueError(f"Expected a non-empty value for `id` but received {id!r}")
        return self._get(
            path_template("/v1/webhooks/{id}/events", id=id),
            options=make_request_options(
                extra_headers=extra_headers,
                extra_query=extra_query,
                extra_body=extra_body,
                timeout=timeout,
                query=strip_not_given(
                    {
                        "limit": limit,
                        "offset": offset,
                        "status": status,
                        "event_type": event_type,
                        "since": since,
                        "until": until,
                    }
                ),
            ),
            cast_to=models.WebhookEventsListResponse,
        )


class AsyncWebhooksResource(AsyncAPIResource):
    async def create_webhook(
        self,
        *,
        url: str,
        subscribed_events: Iterable[
            Literal[
                "task.completed",
                "task.failed",
                "task.cancelled",
                "task.healed",
                "session.completed",
                "session.failed",
                "session.recording.ready",
                "batch.completed",
                "batch.failed",
                "intervention.requested",
                "intervention.resolved",
                "identity.authenticated",
                "identity.authentication_failed",
            ]
        ],
        description: str | NotGiven = not_given,
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> models.WebhookWithSecret:
        """
        Create Webhook

        Register a new HTTPS endpoint to receive events. The response includes
        a one-time `secret` (HMAC-SHA256 signing key) — store it; Anchor never
        returns it again. Up to 5 webhooks per project.

        Args:
          url: HTTPS endpoint Anchor will POST events to. Rejected (400) if it points at
          loopback / private / link-local IPs, cloud metadata services, internal-only
          TLDs, AWS STS / IAM / EKS endpoints, or contains basic-auth credentials.
          description: Optional human-readable description.
          subscribed_events: Events this webhook should receive. At least one is required.
        """
        return await self._post(
            "/v1/webhooks",
            body=strip_not_given({"url": url, "description": description, "subscribed_events": subscribed_events}),
            options=make_request_options(
                extra_headers=extra_headers, extra_query=extra_query, extra_body=extra_body, timeout=timeout
            ),
            cast_to=models.WebhookWithSecret,
        )

    async def list_webhooks(
        self,
        *,
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> models.WebhookListResponse:
        """
        List Webhooks

        List every active webhook for the authenticated project. The signing secret is never
        included.
        """
        return await self._get(
            "/v1/webhooks",
            options=make_request_options(
                extra_headers=extra_headers, extra_query=extra_query, extra_body=extra_body, timeout=timeout
            ),
            cast_to=models.WebhookListResponse,
        )

    async def get_webhook(
        self,
        id: str,
        *,
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> models.WebhookPublic:
        """
        Get Webhook

        Fetch a single webhook config. The signing secret is never returned.

        Args:
          id: Webhook id.
        """
        if not id:
            raise ValueError(f"Expected a non-empty value for `id` but received {id!r}")
        return await self._get(
            path_template("/v1/webhooks/{id}", id=id),
            options=make_request_options(
                extra_headers=extra_headers, extra_query=extra_query, extra_body=extra_body, timeout=timeout
            ),
            cast_to=models.WebhookPublic,
        )

    async def update_webhook(
        self,
        id: str,
        *,
        url: str | NotGiven = not_given,
        description: str | NotGiven = not_given,
        subscribed_events: Iterable[
            Literal[
                "task.completed",
                "task.failed",
                "task.cancelled",
                "task.healed",
                "session.completed",
                "session.failed",
                "session.recording.ready",
                "batch.completed",
                "batch.failed",
                "intervention.requested",
                "intervention.resolved",
                "identity.authenticated",
                "identity.authentication_failed",
            ]
        ]
        | NotGiven = not_given,
        enabled: bool | NotGiven = not_given,
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> models.WebhookPublic:
        """
        Update Webhook

        Partial update — pass only the fields you want to change. Setting `enabled: false`
        immediately stops events from fanning out to this webhook (existing in-flight
        deliveries finish their attempts).

        Args:
          id: Webhook id.
        """
        if not id:
            raise ValueError(f"Expected a non-empty value for `id` but received {id!r}")
        return await self._patch(
            path_template("/v1/webhooks/{id}", id=id),
            body=strip_not_given(
                {"url": url, "description": description, "subscribed_events": subscribed_events, "enabled": enabled}
            ),
            options=make_request_options(
                extra_headers=extra_headers, extra_query=extra_query, extra_body=extra_body, timeout=timeout
            ),
            cast_to=models.WebhookPublic,
        )

    async def delete_webhook(
        self,
        id: str,
        *,
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> models.WebhookDeleteResponse:
        """
        Delete Webhook

        Soft-delete a webhook. New events stop fanning out immediately. The events
        history (`/v1/webhooks/{id}/events`) is still queryable for the retention
        window even after delete.

        Args:
          id: Webhook id.
        """
        if not id:
            raise ValueError(f"Expected a non-empty value for `id` but received {id!r}")
        return await self._delete(
            path_template("/v1/webhooks/{id}", id=id),
            options=make_request_options(
                extra_headers=extra_headers, extra_query=extra_query, extra_body=extra_body, timeout=timeout
            ),
            cast_to=models.WebhookDeleteResponse,
        )

    async def rotate_webhook_secret(
        self,
        id: str,
        *,
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> models.WebhookWithSecret:
        """
        Rotate Webhook Signing Secret

        Mint a new HMAC-SHA256 signing secret. The previous secret remains valid
        for 24 hours so you can update your verification code at your own pace,
        then drop the old secret.

        The new secret is returned exactly once, in the `secret` field.

        Args:
          id: Webhook id.
        """
        if not id:
            raise ValueError(f"Expected a non-empty value for `id` but received {id!r}")
        return await self._post(
            path_template("/v1/webhooks/{id}/rotate-secret", id=id),
            options=make_request_options(
                extra_headers=extra_headers, extra_query=extra_query, extra_body=extra_body, timeout=timeout
            ),
            cast_to=models.WebhookWithSecret,
        )

    async def send_webhook_test_event(
        self,
        id: str,
        *,
        event_type: Literal[
            "task.completed",
            "task.failed",
            "task.cancelled",
            "task.healed",
            "session.completed",
            "session.failed",
            "session.recording.ready",
            "batch.completed",
            "batch.failed",
            "intervention.requested",
            "intervention.resolved",
            "identity.authenticated",
            "identity.authentication_failed",
        ],
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> models.WebhookTestResponse:
        """
        Send Test Event

        Fire a synthetic event of the given type through the real signing + retry
        path. Useful during development for confirming your endpoint can verify
        the signature and is reachable from Anchor's egress IPs.

        The synthetic delivery counts toward your delivery log so you can see it
        in `GET /v1/webhooks/{id}/events`.

        Args:
          id: Webhook id.
          event_type: Catalog of webhook event types Anchor can deliver. Adding a new event
          type is a coordinated change between session-manager (publisher),
          webhook-dispatcher (consumer), and the dashboard.
        """
        if not id:
            raise ValueError(f"Expected a non-empty value for `id` but received {id!r}")
        return await self._post(
            path_template("/v1/webhooks/{id}/test", id=id),
            body=strip_not_given({"event_type": event_type}),
            options=make_request_options(
                extra_headers=extra_headers, extra_query=extra_query, extra_body=extra_body, timeout=timeout
            ),
            cast_to=models.WebhookTestResponse,
        )

    async def list_webhook_events(
        self,
        id: str,
        *,
        limit: int | NotGiven = not_given,
        offset: int | NotGiven = not_given,
        status: Literal["pending", "in_flight", "succeeded", "failed", "dead"] | NotGiven = not_given,
        event_type: Literal[
            "task.completed",
            "task.failed",
            "task.cancelled",
            "task.healed",
            "session.completed",
            "session.failed",
            "session.recording.ready",
            "batch.completed",
            "batch.failed",
            "intervention.requested",
            "intervention.resolved",
            "identity.authenticated",
            "identity.authentication_failed",
        ]
        | NotGiven = not_given,
        since: str | NotGiven = not_given,
        until: str | NotGiven = not_given,
        extra_headers: Headers | None = None,
        extra_query: Query | None = None,
        extra_body: Body | None = None,
        timeout: float | httpx.Timeout | None | NotGiven = not_given,
    ) -> models.WebhookEventsListResponse:
        """
        List Webhook Deliveries

        Paginated history of every delivery attempt for this webhook (succeeded,
        failed, or dead-lettered). Filter by status, event type, or time range.
        Retention: 7 days.

        Args:
          id: Webhook id.
          limit: Page size, 1–100. Defaults to 25.
          offset: Number of rows to skip from the beginning of the result set.
          status: Filter by delivery status.
          event_type: Filter by event type.
          since: Only include deliveries scheduled at or after this ISO 8601 timestamp.
          until: Only include deliveries scheduled at or before this ISO 8601 timestamp.
        """
        if not id:
            raise ValueError(f"Expected a non-empty value for `id` but received {id!r}")
        return await self._get(
            path_template("/v1/webhooks/{id}/events", id=id),
            options=make_request_options(
                extra_headers=extra_headers,
                extra_query=extra_query,
                extra_body=extra_body,
                timeout=timeout,
                query=strip_not_given(
                    {
                        "limit": limit,
                        "offset": offset,
                        "status": status,
                        "event_type": event_type,
                        "since": since,
                        "until": until,
                    }
                ),
            ),
            cast_to=models.WebhookEventsListResponse,
        )
